import css from './NotFoundPage.module.css';

const NotFoundPage = () => {
  return (
    <div className={css.wrap}>
      <h1>Sorry, but there is no page at this address </h1>
    </div>
  );
};

export default NotFoundPage;
